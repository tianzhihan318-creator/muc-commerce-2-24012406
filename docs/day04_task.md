# Day04任务：电商用户数据清洗

使用`data/E Commerce Dataset.xlsx`中的`E Comm`工作表，完成：

1. 清洗前质量报告和初始审计；
2. 缺失值处理中位数基线方案；
3. 类别名称标准化；
4. 完全重复记录检查；
5. `TenureGroup`和`IsMobileLogin`数据转换；
6. 候选异常值报告和业务规则报告；
7. 清洗后质量报告、处理日志和清洗数据输出；
8. 不超过200字的项目复盘。

注意：不得使用`Churn`分组填补特征，不得仅凭IQR自动删除候选异常值。
